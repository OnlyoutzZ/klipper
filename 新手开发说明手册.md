# 新手开发说明手册

> N32H76x 移植项目 · 环境搭建、Git 与固件构建调试入门
>
> 面向刚加入本项目的开发者，整理自实际开发操作与 `使用指南.md`。

## 一、项目概览

本仓库是 Klipper 3D 打印机固件，在官方版本基础上增加了 N32H76x（国民技术，ARM Cortex-M7）移植。

| 目录 / 分支 | 说明 |
| --- | --- |
| `src/n32h7/` | N32H76x 移植源码（串口、GPIO、时钟、启动等） |
| `lib/n32h7xx/` | 国民技术 N32 系列官方库 |
| `test/configs/n32h76x-serial.config` | N32H76x USART1 测试配置 |
| `.config` | 当前构建配置（已纳入版本管理） |
| `.vscode/` | VSCode 一键构建 / 烧录任务 |
| `master` 分支 | 主分支 |
| `development` 分支 | 唯一开发分支，**只允许**在此开发和提交代码 |
| 远程 `origin` | 自己的 GitHub 仓库（https://github.com/OnlyoutzZ/klipper.git） |
| 远程 `upstream` | Klipper 官方仓库（https://github.com/Klipper3d/klipper.git） |

## 二、Git 安装

### 1. Git 安装

```bash
sudo apt update
sudo apt install git
```
### 2. 配置 Git

```bash
git config --global user.email "you@example.com"
git config --global user.name "Your Name"
```
### 3. 配置 SSH 密钥 

```bash
ssh-keygen -t ed25519 -C "你的GitHub邮箱地址"
```

### 4. 添加公钥到你的 GitHub 账户

```bash
cat ~/.ssh/id_ed25519.pub

# 然后，登录 GitHub，进入 Settings -> SSH and GPG keys，点击 New SSH Key，将复制的公钥内容粘贴进去并保存

# 配置完成后，可以用下面命令测试连接，无需输入密码即为成功
ssh -T git@github.com
```

## 二、环境搭建（Ubuntu）

### 1. 获取源码

```bash
git clone git@github.com:OnlyoutzZ/klipper.git
cd klipper
```

### 2. 切换到 development 开发分支

**项目规定：只允许在 development 分支上开发和提交代码，不要在 master 上开发。**

`git clone` 后默认停留在 master，先切换到 development：

```bash
git switch development   # 等价于 git checkout development
git branch               # 确认当前分支为 * development
```

克隆时远端已有 development 分支，`git switch development` 会自动建立本地跟踪分支。

### 3. 安装依赖

Ubuntu：

```bash
./scripts/install-ubuntu-22.04.sh
```

脚本会安装交叉编译工具链并创建 `~/klippy-env` Python 虚拟环境。

### 6. 确认工具就绪

```bash
arm-none-eabi-gcc --version        # ARM 交叉编译工具链
~/klippy-env/bin/python --version  # Python 虚拟环境
```

两个命令都能正常输出版本号即说明环境搭建完成。

## 三、Git 日常操作

### 1. 查看当前状态

```bash
git status        # 查看工作区：哪些文件被修改、哪些是未跟踪的新文件
git branch        # 查看本地分支（当前分支带 * 号）
git remote -v     # 查看远程仓库地址
```

### 2. 切换到 development 分支

```bash
git switch development
```

项目规定只允许在 development 分支开发和提交代码，`git branch` 可以确认当前分支（当前分支带 * 号）。切换分支不会影响工作区里未提交的改动。

### 3. 提交代码

```bash
git add -A                   # 暂存所有改动（-A 表示全部文件）
git status                   # 确认暂存内容无误
git commit -m "模块: 改动描述"   # 提交
```

### 4. 推送到 GitHub

```bash
git push -u origin development
```

`-u` 在首次推送时记录分支与远端的对应关系，之后只需 `git push` 即可。

### 5. 确认推送结果

```bash
git status
```

显示「您的分支与上游分支 'origin/development' 一致」即代表推送成功。

### 6. 常见问题

**Q1：推送时报错 `could not read Username for 'https://github.com'`**

本机没有保存 GitHub 凭据。GitHub 已不再支持账号密码，需要使用 Personal Access Token（PAT）：

1. 打开 https://github.com/settings/tokens 创建 token，勾选 `repo` 权限
2. 手动执行 `git push`，用户名填 GitHub 用户名，密码处粘贴 PAT
3. 本机 credential helper 为 `store`，输入过一次后会保存在 `~/.git-credentials`，之后无需再输

**Q2：想把被 `.gitignore` 忽略的文件提交上去？**

从 `.gitignore` 中删掉对应的规则，再 `git add` 提交即可（本项目已通过这种方式把 `.config` 纳入版本管理）。

**Q3：提交信息写错了 / 提交错了？**

```bash
git commit --amend -m "新的信息"   # 修改最近一次提交的信息
git reset --soft HEAD~1            # 撤销最近一次提交（改动保留在工作区）
```

### 7. 提交信息规范

推荐使用 `<模块>: <改动描述>` 的格式，例如：

```
n32h7: Add N32H76x microcontroller support
config: Track n32h76x build configuration
```

### 8. 与官方上游同步（进阶）

```bash
git fetch upstream               # 拉取官方最新代码（不自动合并）
git merge upstream/master        # 把官方 master 合并进当前分支
git log --oneline upstream/master..master   # 查看官方有哪些新提交
```

## 四、固件构建与调试

### 1. 构建流程

```bash
make menuconfig        # 图形化界面选择 MCU（本项目选 N32H76x）
make clean             # 清理上次构建产物
make -j$(nproc)        # 并行编译，$(nproc) 表示用满所有 CPU 核心
```

编译产物为 `out/klipper.bin`。

### 2. 使用现成测试配置

不想手动配置时，直接复制仓库里已有的测试配置：

```bash
cp test/configs/n32h76x-serial.config .config
make olddefconfig
make -j$(nproc)
```

### 3. VSCode 一键构建 / 烧录

VSCode 中：终端 → 运行任务（Terminal → Run Task）：

- **Klipper: Build N32H76x** —— 配置并编译
- **Klipper: Flash N32H76x (J-Link)** —— 编译后直接烧录
- **Klipper: Menuconfig** / **Klipper: Clean** 等

### 4. 烧录固件（J-Link）

```bash
printf 'r\nloadbin out/klipper.bin 0x15000000\nr\ng\nq\n' | JLinkExe \
  -device N32H760xIx7 -if SWD -speed 4000 -AutoConnect 1 -ExitOnError 1
```

`0x15000000` 是应用固件加载地址（`FLASH_APPLICATION_ADDRESS`，见 `src/n32h7/Kconfig`）。

### 5. 连接 MCU 控制台

固件烧录后，通过串口与 MCU 交互：

```bash
~/klippy-env/bin/python klippy/console.py /dev/ttyACM0 -b 250000
```
allocate_oids count=8
config_analog_in oid=0 pin=PA4
query_analog_in oid=0 clock={clock+1000000} sample_ticks=2000 sample_count=8 rest_ticks=1000000 bytes_per_report=16 min_value=0 max_value=65535 range_check_count=3

- 需在项目根目录执行
- `/dev/ttyACM0` 是串口设备，可能因接线而变化，用 `ls /dev/ttyACM*` 确认
- `-b 250000` 是波特率

```bash
==================== attempting to connect ====================
INFO:root:Starting serial connect
Loaded 84 commands (41eb4bf0-dirty-20260821_152958-nsing / gcc: (15:10.3-2021.07-4) 10.3.1 20210621 (release) binutils: (2.38-3ubuntu1+15build1) 2.38)
MCU config: CLOCK_FREQ=600000000 MCU=n32h76x RECEIVE_WINDOW=192 RESERVE_PINS_serial=PA10,PA9 SERIAL_BAUD=250000 STATS_SUMSQ_BASE=256 STEPPER_OPTIMIZED_EDGE=75 STEPPER_STEP_BOTH_EDGE=1
====================       connected       ====================
002.774: stats count=124 sum=1140303 sumsq=60629523
```

- 使用 `SUPPRESS stats` 命令关闭 `stats` 输出

### 6. Level 1 Build Regression（构建回归测试）

改动代码后验证基础构建是否正常：

```bash
make menuconfig   # 选择你的 MCU
make clean
make -j$(nproc)
```

编译无报错即通过。

## 五、完整开发流程示例

1. `git switch development` —— 确认在 development 分支工作
2. 修改代码
3. `make menuconfig && make clean && make -j$(nproc)` —— 验证编译通过
4. 烧录到板子测试
5. `git add -A && git commit -m "xxx: 改动描述"` —— 提交
6. `git push origin development` —— 推送

---

*本手册整理自实际开发操作与 `使用指南.md`。*
